# Speechos Environment Variables
# Copy this file to .env and fill in the values.
#
#   cp .env.example .env

# HuggingFace token — required for gated models (PyAnnote diarization, etc.)
# Get yours at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# Override hardware tier (optional, default: auto-detect)
# SPEECHOS_TIER=auto

# Override compute mode (optional, default: auto)
# SPEECHOS_COMPUTE_MODE=auto
